Installation
=================================
Direct installation:

.. code-block:: bash
   :linenos:

   pip install rlzoo

Install from the source code on github:

.. code-block:: bash
   :linenos:

   git clone https://github.com/tensorlayer/RLzoo.git
   cd RLzoo
   pip install .