Installation
=================================
Direct installation:

.. code-block:: html
   :linenos:

   pip install rlzoo

Install from the source code on github:

.. code-block:: html
   :linenos:

   git clone https://github.com/tensorlayer/RLzoo.git
   cd RLzoo
   pip install .